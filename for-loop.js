var total = 0;
var limit = 10;
for (var i = total; i < limit; i++){
  total += i;
  /** += will add the number i to the variable total when it increase by 1 everytime in the loop**/
};
console.log(total);